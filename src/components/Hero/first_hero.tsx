'use client'

import './styles.css';
import useScroll from '@/hooks/use-scroll';

export default function FirstHero() {
    const process = useScroll();

    return (
        <div>

        </div >
    )
}
